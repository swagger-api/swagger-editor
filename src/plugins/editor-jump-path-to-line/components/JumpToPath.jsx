import React from 'react';

// legacy name that is already built into SwaggerUI
// render svg icon in SwaggerUI
// onClick of svg icon => jump to line in editor
const JumpToPath = () => {
  // leverage editor-monaco-plugin-actions:
  // editorActions.setJumpToEditorMarker
  // editorActions.clearJumpToEditorMarker
  return <div>JumpToPath component</div>;
};

export default JumpToPath;
